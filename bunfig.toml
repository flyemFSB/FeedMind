# Bun 配置
# 参见 https://bun.sh/docs/configuration

[install]
# 锁定文件：文本格式，可读可 diff
lockfile = "bun.lock"

# 使用隔离模式（类似 pnpm），每个包只能访问其声明的依赖
# 防止 phantom dependency
linker = "isolated"

[install.trusted]
# 需要运行 postinstall 脚本的包
esbuild = true
