# ─── 必需 ─────────────────────────────────────────────────────
# AES 加密密钥，用于加密存储的 API Key 等敏感数据。
# 首次使用后请保持稳定，否则已加密数据无法解密。
# 生成命令：openssl rand -hex 32
ENCRYPTION_KEY=feedmind

# ─── 可选（均有合理默认值）───────────────────────────────────
# APP_ENV=development
# DATABASE_PATH=./data/feedmind.db
# API_HOST=127.0.0.1
# API_PORT=18790
# LOG_LEVEL=debug
# WIKI_DIR=data/wiki
# DISABLE_INGEST_WORKER=1

# 爬虫回调 API 地址（反向代理场景下覆盖默认的 http://localhost:18790）
# API_BASE_URL=http://localhost:18790

# 桌面应用内置 Chromium 的 CDP 端点（crawler-core / Agent 浏览器通过它驱动爬虫窗口）
# 默认 http://127.0.0.1:9333，主进程端口由 CDP_PORT 控制
# CDP_ENDPOINT=http://127.0.0.1:9333
# CDP_PORT=9333
